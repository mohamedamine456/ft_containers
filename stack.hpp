#ifndef STACK_HPP
#define STACK_HPP

#include "namespace.hpp"

template < class T, class Container >
class ft::stack {
	public:
		stack<T>(){}
		//stack<T>( const Container & cont = Container() );
		~stack(){}
};

#endif
